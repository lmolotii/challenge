using System;
using System.Collections.Generic;

namespace Profile.Challenge.Algorithm.Parser
{
    public class WordCounter
    {
        public IEnumerable<(int count, string word)> CountWords(IEnumerable<string> words)
        {
            //TODO: Place your calculation logic here.
            
            return new List<(int count, string word)>();
        }
    }
}